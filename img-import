Programme pour importer une image sur Tkinter à partir de PhotoImage.



#!/usr/bin/python
from tkinter import *
#from PIL import *

#Path = oû est stocker l'image
path = 'img.png'




root = Tk()
img = PhotoImage(file=path)
panel = Label(root, image = img)
panel.pack(side = "bottom", fill = "both", expand = "yes")
root.mainloop()
